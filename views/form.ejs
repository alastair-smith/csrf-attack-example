<!DOCTYPE html>

<html lang="en">

<head>
  <meta charset="UTF-8" />
  <title>Example App</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <meta name="description" content="" />
  <link rel="icon" href="favicon.png">
</head>

<body>
  <h1>Example app</h1>

  <% if (message) { %>
    <span>
      <%= message %>
    </span>
    <% } %>
      <form method="post">
        <label>
          What is your favourite film
          <input name="film" />
          <% if (csrfToken) { %>
            <input name="csrf-token" hidden value="<%= csrfToken %>" />
            <% } %>
              <button>Submit</button>
        </label>
      </form>
</body>

</html>